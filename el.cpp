#include <bits/stdc++.h>
using namespace std;

void solve(){
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // solve();
    vector<int> sub(3);
    sub.push_back(3);

    for(auto x:sub) cout<<x<<" ";
    return 0;


}